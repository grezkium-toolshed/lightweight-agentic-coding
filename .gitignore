# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo

# Local model artifacts — NEVER commit model weights or generated model metadata
models/**

# Backups and generated config copies
backup-configs/
*.bak
*.backup.*
state/**
runtime-config/presets.active.ini
runtime-config/active-profile.txt
runtime-config/opencode.active.json

# Local tool state
.sisyphus/
.planning/
.opencode/render-manifest.json
.opencode/package.json
.opencode/package-lock.json
.opencode/node_modules/
.opencode/skills/msgraph/
.claude/
.qwen/
*.log
*.tmp

# Python cache
__pycache__/
*.pyc

# Python build artifacts
**/*.egg-info/
dist/
build/
*.egg

# Generated at build time from the canonical top-level trees (see scripts/stage_data.py)
src/lac/data/

# Internal working notes (kept locally, never shipped in the public tree)
dev/

# Virtual environments
.venv/
venv/

# Staged customer Assessment run data (privacy-sensitive, never committed)
assessment/

# Internal MSP workflow docs and skill (kept locally, never shipped)
docs/internal/
.opencode/skills/tenant-smith-workflow/

// Cloud-only profile — no local models loaded
// Uses OpenRouter free tier via opencode.jsonc provider block
// Run llama-server with minimal config or skip it entirely

version = 1

[*]
host = 127.0.0.1
port = 8080
flash-attn = true
parallel = 1
